#pragma once

// List of WiFi networks to try in order. Add as many entries as needed.
// Format: { "SSID", "PASSWORD" }
#define WIFI_NETWORKS \
    { "home-network",   "home-password"   }, \
    { "office-network", "office-password" }

#define ROUTER_MDNS_HOST "YOUR_MDNS_HOST_NAME"  // e.g. "mypc" → mypc.local
